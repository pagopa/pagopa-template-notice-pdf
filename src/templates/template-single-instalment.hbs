<!DOCTYPE html>
<html lang="{{#if metadata.language}}{{metadata.language}} {{metadata.secondaryLanguage}}{{else}}'it'{{/if}}" {{#if
    metadata.trueBilingualism}}class="true-bilingualism" {{/if}} xmlns="http://www.w3.org/1999/xhtml" version="0.0.20">

<head>
    <title>{{{languageHandler 'title'}}}</title>
    <meta http-equiv="Content-Type" content="text/html" charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./assets/style.css">
</head>

<body>
    <div class="page">
        <div class="page-wrapper">

            {{!-- Notice HEADER --}}
            {{> header }}

            <main class="main-wrapper">
                {{!-- Row 1 --}}
                {{> mainInfo }}
                {{!-- Row 2 --}}
                {{> paymentInfo }}
                {{!-- Row 3 - pagoPA QR Code --}}
                {{#with notice}}
                {{> paymentData
                debtor=../debtor
                notice=../notice
                payee=../payee
                tempPath=../tempPath
                }}
                {{/with}}

            </main>
        </div>
    </div>

</body>

</html>